name: his_mobile
description: "HIS Pro Mobile - Khoa Cap Cuu"
publish_to: 'none'
version: 3.0.131+275
environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # v2.87.0: Google Fonts - há»— trá»£ tiáº¿ng Viá»‡t Ä‘áº§y Ä‘á»§ (Roboto)
  google_fonts: ^6.2.1

  # State Management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

  # Network
  dio: ^5.4.3+1
  cookie_jar: ^4.0.8
  dio_cookie_manager: ^3.1.1

  # Local Storage
  shared_preferences: ^2.2.3
  path_provider: ^2.1.6
  # v3.0.34: Secure storage cho Bearer token (Android Keystore / iOS Keychain)
  flutter_secure_storage: ^9.2.4

  # Navigation
  go_router: ^14.2.0

  # WebView
  webview_flutter: ^4.7.0
  syncfusion_flutter_pdfviewer: ^33.2.13
  pdf: ^3.12.0
  printing: ^5.14.3
  permission_handler: ^12.0.3

  # v2.37.0: OpenVPN/WiFi Settings
  url_launcher: ^6.3.0

  # v3.0.76: Native OpenVPN client (ics-openvpn) - tÃ­ch há»£p trong app
  openvpn_flutter: ^1.3.4

  # v2.36.2: QR Scanner
  mobile_scanner: ^5.2.3

  # v2.41.0: Scan phieu - capture image + signature
  image_picker: ^1.1.2
  signature: ^5.5.0
  path: ^1.9.0

  # v3.0.24: SHA-256 hash cho VNPT SmartCA workflow
  crypto: ^3.0.3

  # v2.66.0: Láº¥y version app Ä‘á»™ng (hiá»ƒn thá»‹ á»Ÿ Settings)
  package_info_plus: ^8.0.2
  # v3.0.34: Audit trail - device info
  device_info_plus: ^12.0.0
  # v3.0.58: Share log file qua Zalo/Facebook/Email
  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/vpn/his_vpn.ovpn
    - assets/vpn/nemk_vpn.ovpn
    - assets/fonts/
